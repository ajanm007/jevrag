"""Vendored, self-contained copies of small modules JevRAG depends on.

Each module here notes its real origin and why it was copied rather than
imported live. Kept here specifically so this package installs and runs
standalone, without requiring an external, separately-obtained checkout.
"""
